version: "3.9"

services:
  server:
    build:
      context: ./server
    ports:
      - "5000:5000"
    env_file:
      - ./server/.env
    volumes:
      - ./server/uploads:/app/uploads

  client:
    build:
      context: ./client
    ports:
      - "3000:80"
    depends_on:
      - server
